-- 恢复被错误标记为「已付款」的供应商记录
-- 执行时间：2026-04-06
-- 说明：由于之前的 bug，确认付款时只根据 project_id 更新，导致同一项目的所有供应商都被标记为已付款
-- 这个脚本会尝试恢复被错误更新的数据

-- 方法：找到在结算单付款后，同一项目其他供应商的记录，将它们的状态改回「未付款」
-- 注意：这个脚本需要根据实际情况调整，因为无法准确知道哪些记录是错误更新的

-- 查看受影响的记录（先查询，确认无误后再执行更新）
-- 这里列出的记录是被错误标记为「已付款」的非餐饮供应商

-- 示例：如果你想恢复特定项目的记录，可以这样做：
/*
UPDATE project_financial_suppliers
SET payment_status = '未付款'
WHERE project_id = '特定项目 ID'
  AND supplier_id != '餐饮供应商 ID'
  AND payment_status = '已付款';
*/

-- 更安全的做法是手动检查每条记录，因为无法准确判断哪些是正确的「已付款」状态

